Django==5.1.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-filter==24.3
django-cors-headers==4.4.0
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
psycopg2-binary==2.9.10
Pillow==11.1.0
python-dotenv==1.0.1
google-auth==2.39.0
pypdfium2==4.30.1
whitenoise==6.9.0
cryptography==46.0.5
django-storages==1.14.5
boto3==1.35.88

# ── Security hardening additions ────────────────────────────────────────────
# pyotp: TOTP MFA (not used — implemented natively in apps/accounts/mfa.py)
# user-agents: server-side UA parsing for device fingerprinting
user-agents==2.2.0

# ── Push notifications ───────────────────────────────────────────────────────
# Firebase Admin SDK for FCM push to Android / iOS / web.
# Requires FIREBASE_SERVICE_ACCOUNT_KEY env var (JSON) or FIREBASE_SERVICE_ACCOUNT_KEY_PATH.
firebase-admin==6.5.0
