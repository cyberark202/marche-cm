apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "marche-cm.fullname" . }}-worker-financial
  labels:
    {{- include "marche-cm.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker-financial
spec:
  # Réplique UNIQUE + concurrence 1 (args) : intégrité financière, aucun
  # parallélisme sur la file `financial`. Recreate pour ne jamais avoir 2 pods.
  replicas: {{ .Values.workerFinancial.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "marche-cm.selector" (merge (dict "component" "worker-financial") .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "marche-cm.labels" . | nindent 8 }}
        app.kubernetes.io/component: worker-financial
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "marche-cm.serviceAccountName" . }}
      securityContext:
        {{- include "marche-cm.podSecurityContext" . | nindent 8 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: worker
          image: {{ include "marche-cm.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- include "marche-cm.containerSecurityContext" . | nindent 12 }}
          args:
            {{- toYaml .Values.workerFinancial.args | nindent 12 }}
          envFrom:
            {{- include "marche-cm.envFrom" . | nindent 12 }}
          resources:
            {{- toYaml .Values.workerFinancial.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
